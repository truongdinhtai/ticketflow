# Production build.
# Empty base URL => same-origin paths proxied by Nginx (see nginx.conf).
# If you serve the API on a different origin instead, set the full URL here,
# e.g. VITE_API_BASE_URL=https://api.your-domain.com  (then enable CORS there).
VITE_API_BASE_URL=
