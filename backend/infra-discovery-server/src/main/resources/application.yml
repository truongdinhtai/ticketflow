spring:
  application:
    name: discovery-server
  config:
    # Modern Spring Cloud way (replaces legacy bootstrap.yml): pull config from
    # the Config Server. "optional:" so the app can still start if it is down.
    import: ${CONFIG_IMPORT:optional:configserver:http://localhost:8888}
